<div class="post-list">
  {{#each posts}}
  <div>
    <h3>
      <a href="/posts/{{id}}">{{title}}</a>
    </h3>

    <p>User {{User.username}} on {{format_date createdAt}}</p>
  </div>

  <div>{{body}}</div>
  {{/each}}
</div>